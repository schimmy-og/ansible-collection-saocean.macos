---
- hosts: localhost
  connection: local
  roles:
    - role: geerlingguy.mac.homebrew
      vars:
        homebrew_installed_packages:
          - gawk
          - gnu-indent
          - gnu-sed
          - gnu-tar
          - gnu-time
          - gnu-which
          - gnupg
          - grep
          - pyenv
    - role: saocean.macos.role_podman